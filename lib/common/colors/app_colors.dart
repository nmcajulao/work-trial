import 'package:flutter/material.dart';

class AppColors {
  // * Card background Color
  static const Color cardBackground = Color(0xFFFBFAFC);

  // * Country List Header Font Color
  static const Color fontPrimaryColor = Color(0xFF2D1F52);
  static const Color fontPrimaryLightColor = Color(0xFF966AFF);

  // * Labels
  static const Color fontLabelColor = Color(0xFF7A7487);
}
